# Core Application Dependencies

# Frontend — pinned to patched release (2.3.0 had a known XSS vulnerability)
chainlit==2.11.1

# Backend API
fastapi
uvicorn[standard]

# Google Cloud & ADK (used for Gemini pipeline path)
google-adk
google-cloud-firestore
google-cloud-storage

# HuggingFace Inference (Gemma model path)
huggingface_hub>=0.26.0

# Utilities
python-dotenv
jinja2
pydantic
pydantic-settings

# Development/Testing
pytest
pytest-asyncio
